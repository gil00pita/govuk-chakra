import { Meta, Canvas, Controls, Unstyled } from '@storybook/addon-docs/blocks'
import * as DesignSystemStories from './DesignSystem.stories'
import { CodeBlock, Table } from '@/components'

<Meta title="GOV.UK/Styles/Design System/Overview" of={DesignSystemStories} />

# Design System

The GOV.UK Design System is a set of design patterns and components that are used to create consistent and accessible user interfaces for government services.

<Canvas of={DesignSystemStories.ColorPalette} />
